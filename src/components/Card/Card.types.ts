/**
 * @file Card.types.ts
 * @description Type definitions for the Card wrapper component.
 */
import type { CardProps as AntCardProps } from 'antd'

/** Props for the Card wrapper. Extends Ant Design CardProps. */
export interface CardProps extends AntCardProps {}
