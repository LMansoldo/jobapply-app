/**
 * @file Modal.types.ts
 * @description Type definitions for the Modal wrapper component.
 */
import type { ModalProps as AntModalProps } from 'antd'

/** Props for the Modal wrapper. Extends Ant Design ModalProps. */
export interface ModalProps extends AntModalProps {}
