/**
 * @file Carousel.styles.ts
 * @description Emotion styles for the Carousel wrapper component.
 */
import { css } from '@emotion/css'

// Note: Carousel styling is typically done via Ant Design's theme system
// or by overriding CSS classes. This file is kept for consistency with
// other component wrappers, but may not be needed for basic usage.

export const carousel = css({
  // Base styles can be added here if needed
})