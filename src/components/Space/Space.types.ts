/**
 * @file Space.types.ts
 * @description Type definitions for the Space wrapper component.
 */
import type { SpaceProps as AntSpaceProps } from 'antd'

/** Props for the Space wrapper. Extends Ant Design SpaceProps. */
export interface SpaceProps extends AntSpaceProps {}
